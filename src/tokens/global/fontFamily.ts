export type FontFamilyProps = {
  text: string;
  code: string;
};

const fontFamily: FontFamilyProps = {
  text: 'Formular, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol"',
  code: "monospace",
};

export default fontFamily;
